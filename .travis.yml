language: php
php:
  - 5.4
  - 5.5
  - 5.6
  - hhvm
matrix:
  allow_failures:
    - php: hhvm
  fast_finish: true

before_script:
  - composer selfupdate && composer --version
  - composer update --dev --prefer-dist

cache:
  directories:
    - vendor
